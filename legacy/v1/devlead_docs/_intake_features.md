# Feature Intake

> Type: INTAKE
> Last updated: 2026-04-05 | Open: 48 | Closed: 1

## Active

| Key | Item | Source | Added | Status | Priority | Notes |
|-----|------|--------|-------|--------|----------|-------|
| FEAT-001 | Token usage KPIs -- cost per task, wasted tokens, efficiency | user | 2026-04-05 | OPEN | P3 | V2 -- needs Claude transcript_path from hook input |
| FEAT-002 | Claude Workflow hooks -- governance for scheduled agents | user | 2026-04-05 | OPEN | P3 | V2 -- pre/post workflow gates, DoD for scheduled tasks |
| FEAT-003 | Rollover policy -- by date OR by file size (lines threshold) | user | 2026-04-05 | OPEN | P2 | Config: rollover_trigger = "date" or "size", max_lines = 500 |
| FEAT-004 | Cross-project guard -- user-level hook protects all DevLead projects | user | 2026-04-05 | OPEN | P1 | `devlead install-guard` sets up ~/.claude/settings.json hook. Prevents ungoverned edits from other sessions. |
| FEAT-005 | File path enforcement in gate -- inspect stdin JSON for target path | user | 2026-04-05 | OPEN | P1 | Gate checks WHERE files are written, not just IF. Blocks memory writes outside UPDATE, enforces naming convention. |
| FEAT-006 | Session-end HTML dashboard -- beautiful local report with all KPIs, project updates, stats | user | 2026-04-05 | OPEN | P1 | Generated when session ends. Clickable, dashboard-style. Every detail: KPIs, audit log, tasks done, state transitions, intake changes. |
| FEAT-007 | Token usage auditing -- full capability for tracking, waste detection, best practices | user | 2026-04-05 | OPEN | P1 | Read transcript_path from hook stdin. Detect wasteful patterns: repeated image attachments, large context, bad prompts. Show cost per task, wasted tokens, efficiency recommendations. Include in HTML dashboard. |
| FEAT-008 | Scope lock -- user defines surgical focus for what LLM must work on | user | 2026-04-05 | OPEN | P1 | User specifies allowed files, directories, or approach before EXECUTE. Gate enforces scope -- blocks edits outside allowed paths. Prevents AI from wandering into wrong files or reworking things not asked for. Could be `devlead scope set src/devlead/audit.py src/devlead/cli.py` or a scope section in the plan. |
| FEAT-009 | Record model name everywhere -- audit log, session history, HTML dashboard | user | 2026-04-05 | OPEN | P2 | Users choose models (Claude, GPT, Copilot). Record which model was used in every audit entry and session. Enables per-model KPI comparison (which model wastes more tokens, which has better FTR). |
| FEAT-010 | API key / license management -- free vs Pro tier gating | user | 2026-04-05 | OPEN | P1 | Validate license key for Pro features (portfolio, collab). Free tier = 1 project, full features. Pro = unlimited projects. Key stored locally, no server call. |
| FEAT-011 | HTML dashboard → markdown updates -- interactive edits from browser | user | 2026-04-05 | OPEN | P2 | Basic update forms in HTML dashboard that write back to allowed devlead_docs/ MD files. E.g., mark a task DONE, close an intake item, add a note. Local file writes only. |
| FEAT-012 | GitHub open-source packaging -- LICENSE, proper README, PyPI distribution | user | 2026-04-05 | OPEN | P1 | MIT license, proper README.md, setup for PyPI distribution. Make repo presentable for open-source. |
| FEAT-013 | Portfolio dashboard -- multi-project view with project selector | user | 2026-04-05 | OPEN | P2 | Dashboard should handle portfolio context. Project selector at top, cross-project stats, per-project drill-down. Hierarchy: Portfolio → Project → Epic → Story → Task. |
| FEAT-014 | Progress timeline -- only TBO milestones and productionization | user | 2026-04-05 | OPEN | P1 | Trends timeline tracks ONLY TBO status changes and distribution milestones. Technical work (stories/tasks) is shadow work -- work that doesn't contribute to any TBO is shadow work in hindsight. Timeline should be sparse: each entry = a TBO moved. |
| FEAT-015 | Split dashboard.py into smaller modules | user | 2026-04-05 | OPEN | P2 | dashboard.py is 1200+ lines. Split into: dashboard_html.py (CSS/wrapper), dashboard_tabs.py (tab builders), dashboard_helpers.py (formatting). Keep each under ~200 lines. |
| FEAT-016 | Multi-LLM adapters -- Cursor, Copilot, Windsurf, Aider | user | 2026-04-05 | OPEN | P2 | Core engine is LLM-agnostic. Need thin adapters that translate each tool's hook/plugin system into devlead gate calls. Claude adapter is built. Each adapter intercepts "about to edit" and calls devlead gate. |
| FEAT-017 | `devlead gap` command -- automated governance gap detection | user | 2026-04-05 | OPEN | P1 | Scans devlead_docs/ for: missing cross-references, files referenced but absent, roadmap vs task alignment, file size violations. Outputs gap report. Optionally updates _intake_gaps.md. Replaces manual auditing. |
| FEAT-018 | Revenue model -- Free/$29 Pro (5 projects)/$49 Unlimited tiers | user | 2026-04-05 | OPEN | P1 | Yearly subscription. Lapse = downgrade to Free, extra projects dormant. License key via lightweight API. Builds on FEAT-010. |
| FEAT-019 | Lightweight license API server -- 3 endpoints for purchase/verify/check | user | 2026-04-05 | OPEN | P2 | POST /license/activate, GET /license/verify, GET /license/check. Stripe webhook, SQLite, single Python file. Offline-first -- CLI checks locally, pings server weekly. |
| FEAT-020 | Enforce "update docs before proposing work" -- gate that prevents stale snapshots | user | 2026-04-05 | OPEN | P1 | AI must update intake/status/tasks BEFORE proposing a sequence of work. Could be a checklist item in ORIENT or a gate in the state machine. Prevents planning on stale data. |
| FEAT-021 | Scratchpad -- temporary capture space before triage | user | 2026-04-05 | OPEN | P1 | Quick-capture for ideas, bugs, observations that haven't been triaged yet. Lives in devlead_docs/_scratchpad.md or similar. Items sit here until explicitly triaged into _intake_features.md, _intake_bugs.md, _intake_gaps.md. Prevents losing context while working. |
| FEAT-022 | Intake triage workflow -- scratchpad → backlog via triage | user | 2026-04-05 | OPEN | P1 | `devlead triage` command or INTAKE state checklist. Reads scratchpad, classifies as feature/bug/gap with priority. Output goes to backlog (proper intake files). Clears scratchpad items after triage. Backlog = _intake_features.md, _intake_bugs.md, _intake_gaps.md. |
| FEAT-023 | Surveillance -- enforce Task→Story→TBO linkage | user | 2026-04-05 | OPEN | P1 | Every task must tag its parent story. Every story must link to a TBO. `devlead gap` or a dedicated `devlead surveillance` command checks for orphaned tasks (no story) and orphaned stories (no TBO). Unlinked work = shadow work. Could run as part of ORIENT or UPDATE checklist. |
| FEAT-024 | Definition of Done gate -- require DoD before implementation | user | 2026-04-05 | OPEN | P1 | Before any story moves to EXECUTE, a Definition of Done must exist -- either user-provided or model-proposed (approved by user). DoD lives in _project_stories.md per story. Without DoD, a TBO cannot be marked complete because there's no criteria to verify. Gate blocks PLAN→EXECUTE if active story has no DoD. |
| FEAT-025 | Custom state machine steps -- user-defined states and gates | user | 2026-04-05 | OPEN | P1 | Users should be able to add custom steps to the state machine via devlead.toml. E.g., add a TEST step after EXECUTE, or a REVIEW step before UPDATE. Each custom step can have its own checklist and gate rules. Enables workflows like "always run tests after major work." |
| FEAT-026 | Intake-to-Story promotion -- FEAT items become stories when triaged to backlog | user | 2026-04-05 | OPEN | P1 | FEAT-* is not a story -- it's an intake item (raw idea). Triage promotes it to a Story (S-*) in _project_stories.md with proper fields: DoD, TBO link, priority, risks. Unpromoted FEATs stay in backlog. This clarifies the lifecycle: Scratchpad → Intake (FEAT/BUG/GAP) → Triage → Story → Task → TBO. |
| FEAT-027 | No-work-without-task gate -- block Edit/Write if no IN_PROGRESS task exists | user | 2026-04-05 | OPEN | P1 | Gate hook reads _project_tasks.md, checks for at least one IN_PROGRESS task. No active task = block. Configurable enforcement (log/warn/block). Forces Claude to pick work from the book of work, never ad-hoc. |
| FEAT-028 | Mandatory ID chain -- every task must have Story + TBO IDs, unlinked = shadow work | user | 2026-04-05 | OPEN | P1 | Model is responsible for assigning proper Story ID and TBO ID to every task. If a task has no Story link → shadow work. If a Story has no TBO link → shadow work. Convergence = TBOs done / TBOs total. No gaps allowed in convergence reporting. `devlead report` and `devlead gap` must flag unlinked tasks and compute shadow work %. |
| FEAT-029 | ON_HOLD task status -- user decision to pause work for reprioritization | user | 2026-04-05 | OPEN | P1 | Tasks can be marked ON_HOLD when user decides to reprioritize. Model cannot put a task on hold -- only the user. ON_HOLD tasks don't count as IN_PROGRESS for the task gate. When resuming, user explicitly moves back to IN_PROGRESS. Prevents model from silently abandoning work. |
| FEAT-030 | Story acceptance -- story done when user accepts, not when tasks complete | user | 2026-04-05 | OPEN | P1 | All tasks done ≠ story done. Story is done when the deliverable can be shipped and user explicitly accepts it. Model must understand lineage (Task→Story→TBO) and DoD before starting work. Story status: OPEN → IN_PROGRESS → REVIEW → ACCEPTED. Only user can move to ACCEPTED. Refines FEAT-024 and FEAT-028. |
| FEAT-031 | MVP phases -- enforce scope boundaries to prevent goalpost shifting | user | 2026-04-05 | OPEN | P1 | DevLead should enforce phases (MVP1, MVP2, etc.) so scope doesn't creep indefinitely. Each phase has a fixed set of stories/TBOs. New requests during a phase go to next phase backlog, not current. Prevents "just one more thing" from blocking delivery. Phase gate: no new stories added to current phase after kickoff. |
| FEAT-032 | Task-level effort tracking -- hours, sessions, tokens captured per task and aggregated to story | user | 2026-04-05 | OPEN | P1 | Each task tracks: wall-clock hours (session start/end timestamps), # of sessions worked, tokens used (from hook stdin transcript_path). Story-level = sum of child tasks. Enables: cost per story, efficiency per model, shadow work cost. Builds on FEAT-007 (token auditing) and session_history.jsonl. |
| FEAT-033 | Auto-generate non-functional tasks -- tests, docs, refactoring created by DevLead | user | 2026-04-05 | OPEN | P1 | When a functional task is created, DevLead auto-generates paired non-functional tasks: write tests (NF), update docs (NF), review for refactoring (NF). Model writes these itself as part of task breakdown. Configurable in devlead.toml -- which NF tasks are required per functional task. Gate: functional task cannot be marked DONE if its paired NF tasks aren't DONE. Prevents skipping tests. |
| FEAT-034 | User-facing enforcement -- all DevLead decisions surface to user, not consumed by model | user | 2026-04-05 | OPEN | P1 | Currently DevLead sends systemMessage to model and model decides what to show. User never sees gates fire. Fix: DevLead blocks should prompt the user for a decision (proceed/deny/override). Warnings should be visible to user, not silently consumed by model. Model should not be the one interpreting DevLead enforcement -- user should be. |
| FEAT-035 | Wholesome project view -- TBO→Story→Task hierarchy in tabular format | user | 2026-04-05 | OPEN | P1 | When showing book of work or priorities, display the full lineage: TBO at top, linked Stories below, Tasks under each Story. Tabular format. Never lose the bigger picture -- we don't code for fun of it. Every task must show how it contributes upward. `devlead report` and `devlead status` should show this hierarchy, not flat lists. |
| FEAT-036 | Visual lineage feedback -- every task shows its business outcome contribution | user | 2026-04-05 | OPEN | P1 | Strong visual feedback on every task back to how it contributes to business outcome. When a task is picked up, displayed, or reported -- show TBO→Story→Task chain. Color-coded: green if chain is complete, red if orphaned (shadow work). Makes shadow work impossible to ignore. |
| FEAT-037 | Smart project analysis -- open-ended questions trigger full project scan | user | 2026-04-05 | OPEN | P1 | When user asks broad questions ("how soon can we ship?", "what's blocking revenue?", "what should we prioritize?"), DevLead should: (1) scan all project docs -- TBOs, stories, tasks, gaps, distribution, intake, (2) capture findings in scratchpad, (3) present a structured analysis tied to business outcomes -- not a flat answer. The analysis should show: which TBOs are blocked and why, what the critical path is, what's shadow work vs real progress. This is the core intelligence of DevLead -- project awareness, not just gate enforcement. |
| FEAT-039 | Auto-publish HTML dashboard during session for visual feedback loop -- user shouldn't stare at terminal | triage | 2026-04-05 | OPEN | P1 | Triaged from scratchpad |
| FEAT-040 | Audit tab - dont show .py files. Define clear objective for the tab. Should show governance enforcement actions, not raw file edits. | triage | 2026-04-05 | OPEN | P2 | Triaged from scratchpad |
| FEAT-041 | KPIs tab is all wrong - need to review what KPIs make sense, which are broken, which should be removed | triage | 2026-04-05 | OPEN | P1 | Triaged from scratchpad |
| FEAT-042 | Interactive server (devlead serve) - add to book of work, do not implement now. TASK-040 created but park it. | triage | 2026-04-05 | OPEN | P2 | Triaged from scratchpad |
| FEAT-043 | DevLead feature: gate that blocks task creation unless scratchpad item has been triaged by user. Model captures in scratchpad, user triages and prioritizes, only then can a TASK be created. Prevents model from self-prioritizing. | triage | 2026-04-05 | OPEN | P1 | Triaged from scratchpad |
| FEAT-044 | DevLead feature: auto-raise tickets for gaps/bugs observed in real-time during session. When DevLead detects an issue (e.g. gap command finds problems, audit detects deviation, state machine skip), it should create a ticket immediately in the right intake file. Not wait for model to remember. | triage | 2026-04-05 | OPEN | P1 | Triaged from scratchpad |
| FEAT-045 | Session tab needs rethink - what question does it answer? Currently shows raw state transitions and checklists which are not meaningful to the user. Should show: what was accomplished this session tied to TBOs, how many tasks moved, time spent. | triage | 2026-04-05 | OPEN | P2 | Triaged from scratchpad |
| FEAT-046 | Post-migration cleanup -- `devlead migrate --cleanup` or `devlead cleanup` command | user | 2026-04-05 | OPEN | P1 | After migration, old governance md files (docs/ROADMAP.md, TASKS.md, etc.) stay in git and confuse AI assistants. Need a cleanup step that: (1) reads content from detected old governance files, (2) merges/appends into corresponding devlead_docs/ file, (3) replaces old file with one-line redirect stub pointing to devlead_docs/, (4) optionally adds old paths to .gitignore, (5) stages changes in git. Could be a --cleanup flag on migrate or a separate command. Non-destructive by default (redirect stubs, not deletion). |
| FEAT-047 | Claude Code plugin packaging -- restructure repo as official Claude Code plugin | brainstorm | 2026-04-05 | OPEN | P1 | Add plugin.json manifest, bin/ wrapper, hooks/ shell scripts, commands/ (dashboard, triage, report), skills/devlead_governance.md. Repo root becomes the plugin. SessionStart hook auto-bootstraps venv in ${CLAUDE_PLUGIN_DATA}. Python 3.11+ required on user machine. |
| FEAT-048 | Plugin self-onboarding -- auto-detect ungoverned projects on SessionStart | brainstorm | 2026-04-05 | OPEN | P1 | When plugin SessionStart fires in a project with no devlead_docs/, scan project, report findings, ask user to approve migration. No manual devlead migrate needed. Hybrid approach: scan + report + wait for approval. |
| FEAT-049 | DevLead-Superpowers integration -- brainstorming/planning skills must read devlead_docs | brainstorm | 2026-04-05 | OPEN | P1 | DevLead feeds Superpowers. Brainstorming should check active task/story context from devlead_docs/. Specs should reference Story ID and TBO. Plans should tag Task ID. Options: (A) DevLead ships its own skills, (B) DevLead exposes data Superpowers consumes, (C) merge them. |

## Archive

| Key | Item | Resolved | Resolution |
|-----|------|----------|------------|
| FEAT-038 | Migration bootstrap -- devlead migrate for existing projects | user | 2026-04-05 | OPEN | P1 | Existing projects may have md files scattered.  scans for existing devlead_docs/, CLAUDE.md, or similar. Maps existing files to DevLead structure. Creates missing governance files. Generates a migration report showing what was found, what was created, what needs manual review. Non-destructive -- never deletes existing files. |
